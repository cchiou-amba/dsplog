# SPDX-License-Identifier: GPL-2.0
#
# Ambarella DSPLOG Kernel Module Kbuild

obj-m := dsplog.o

dsplog-objs := dsplog_drv.o \
	dsplog_timer.o

ccflags-y := -I$(src)/include -I$(src)/include/specific -I$(src)
